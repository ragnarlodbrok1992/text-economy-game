#import "Basic";

main :: () {
  print("texteconomy - alpha build.\n");

  // Print a rectangle area using ASCII codes
  width :: 10;
  height :: 5;

  // Test of some ascii characters in JAI
  print("%\n", cast (u8) 192);
}
